/**
* @ngdoc overview
* @name  Scorecards.common.watch
* @description
* Scorecards.common.watch module - separate module for helpers Service around the $watch mechanism
*/

(function(angular) {
	'use strict';

	angular
		.module('Scorecards.common.watch', [])
		.config(config);

	function config() {
	}

})(window.angular);
